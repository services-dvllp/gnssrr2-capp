/* sleep.h stub for Linux userspace (__BAREMETAL__ path of XRFdc) */
#ifndef SLEEP_H
#define SLEEP_H
#include <unistd.h>
/* Xilinx standalone sleep() is in seconds; maps directly to POSIX sleep(). */
/* usleep() is already provided by unistd.h. */
#endif /* SLEEP_H */
